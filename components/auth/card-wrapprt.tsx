import { Children } from "react";

interface CardWarpperProps{
    children:React.ReactNode;
    headderLabel:string;
    backButtonLabel:string;
    backButtonHref:string;
    showSocial?:boolean;
}
export const CardWarpper=({Children,headerLabel,backButton,backButtonHref,showSocial}:CardWarpperProps)=>{
    
}